FROM python:3.11-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
    git build-essential ripgrep ca-certificates curl && \
    rm -rf /var/lib/apt/lists/*
WORKDIR /workspace
RUN useradd -ms /bin/bash sandbox
USER sandbox
